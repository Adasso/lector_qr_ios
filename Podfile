# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'computerRegister' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'
  pod 'Firebase/Database'

  # Pods for computerRegister

  target 'computerRegisterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'computerRegisterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
